# ============================================
# WebSocket API - Electric Automatic Chile
# Variables de Entorno
# ============================================

# Entorno
NODE_ENV=production
PORT=5000
LOG_LEVEL=info

# JWT Secret (IMPORTANTE: Debe ser el mismo que en Backend API)
JWT_SECRET=tu_jwt_secret_aqui

# URLs de Servicios
MAIN_API_URL=http://localhost:4000
FRONTEND_URL=http://localhost:3000

# CORS Origins (separados por coma)
CORS_ORIGINS=http://localhost:3000,https://tudominio.com

# Redis (opcional - para múltiples instancias)
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=false

# WebSocket Configuration
WS_PING_TIMEOUT=60000
WS_PING_INTERVAL=25000
WS_MAX_CONNECTIONS=1000
